#!/usr/bin/env python3
# Compare local AUR package trees against upstream for the Waybar Arch icon.
# Prints JSON (`text`, `class`, `alt`, `tooltip`). See README.md.
# --refresh  ignore 15s cache    --notify  refresh, notify-send, RTMIN+11

from aur_versions.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
